import { css } from 'styled-components'
import { COLOR } from '../reference-tokens'

export const BOX_SHADOW_CSS = css`
	box-shadow: -0.01rem 0.01rem 0.1rem ${COLOR.grayScale[100]};
`

export const TEXT_SHADOW_CSS = css`
	text-shadow: -0.05rem 0.05rem 0.2rem ${COLOR.grayScale[100]};
`
